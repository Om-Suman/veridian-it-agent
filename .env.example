# Veridian Corp — Internal IT Service Agent Environment Configuration

# Database Configuration (SQLite by default, replaceable with PostgreSQL)
DATABASE_URL=sqlite:///./veridian.db

# Server Configuration
ENV=development
HOST=0.0.0.0
PORT=8000

# Optional External LLM Keys (Deterministic Policy Engine runs locally if left blank)
GEMINI_API_KEY=
OPENAI_API_KEY=

# Frontend Configuration
VITE_API_URL=http://localhost:8000

